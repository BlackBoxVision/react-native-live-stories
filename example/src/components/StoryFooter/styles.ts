import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    color: '#FFF',
  },
  inputContainer: {
    borderColor: '#FAFAFA',
    borderRadius: 50,
    borderWidth: 1,
    paddingHorizontal: 16,
  },
  sendIcon: {
    marginLeft: 8,
  },
});
